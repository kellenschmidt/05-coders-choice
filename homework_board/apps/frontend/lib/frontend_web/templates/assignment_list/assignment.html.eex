<div class="card text-dark mb-3">
  <div class="card-body">
    <h4 class="card-title"><%= get_title(@assignment) %><%= content_tag(:span, get_priority(@assignment), [class: "badge badge-" <> get_color(@column) <> " ml-2"]) %></span></h4>
    <div>
      <%= for label <- get_labels(@assignment) do %>
        <%= content_tag(:span, get_title(label), [class: "badge badge-pill badge-" <> get_color(label) <> " ml-2"]) %>
      <% end %>
    </div>
    <p class="card-text"><%= get_description(@assignment) %></p>
    <%= link("Edit", to: new_assignment_path(@conn, :edit, get_assignment_num(@assignment)), method: "get", class: "card-link") %>
    <%= link("Mark completed", to: "/new", method: "get", class: "card-link") %>
  </div>
</div>
